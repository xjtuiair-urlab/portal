---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import PublicationCard from '../components/PublicationCard.astro';
import { getEntry } from 'astro:content';


const rawBase = import.meta.env.BASE_URL;
const baseUrl = rawBase.endsWith('/') ? rawBase.slice(0, -1) : rawBase;
const getLink = (path: string) => {
    const cleanPath = path.startsWith('/') ? path.slice(1) : path;
    if (cleanPath === '') return `${baseUrl}/`;
    return `${baseUrl}/${cleanPath}`;
}

const statsEntry = await getEntry('homepage', 'stats');
const pubsEntry = await getEntry('homepage', 'pubs');

const stats = statsEntry ? statsEntry.data : [];
const pubs = pubsEntry ? pubsEntry.data : [];
---

<Layout title="Home">
    
    <Hero />

    <section class="research-section">
        <div class="container">
            <div class="research-grid">
                <div class="research-text">
                    <h2 class="content-title">Laboratory Overview</h2>

                    <p>
                        Established in  2025, our laboratory is part of the research group led by Prof. Pengju Ren 
                        at Xi’an Jiaotong University. The lab focuses on frontier research in embodied intelligence, 
                        brain-inspired multimodal foundation models, and the application of emerging computing 
                        architectures to large-scale AI systems. By integrating advances in learning algorithms, 
                        robotic systems, and hardware design, we aim to develop intelligent systems that are both 
                        cognitively grounded and computationally efficient.
                    </p>

                    <p>
                        The laboratory maintains close and active collaborations with interdisciplinary partnerships, 
                        which enable joint exploration across 
                        artificial intelligence, neuroscience, robotics, and computing systems, fostering a research 
                        environment that bridges theory, experimentation, and real-world deployment.
                    </p>

                    <p>
                        Our team is equipped with top-tier computing resources, ensuring sufficient per-capita computational capacity to support 
                        large-scale model training and experimentation. In addition, the laboratory hosts a mature robotic testing 
                        platform and is supported by stable and adequate research funding, enabling sustained 
                        investment in both computational and physical research infrastructures.
                    </p>
                    <a href={getLink("research")} class="btn">More Information</a>
                </div>
                <!-- <div class="research-img">
                    <img src={getLink("assets/background/chip.png")} alt="Research" class="brain-img">
                </div> -->
            </div>
        </div>
    </section>

    <div class="team-section" style={`background-image: url(${getLink("assets/background/member-photo.png")})`}>
        <div class="container">
            <div class="stats-bar">
                {stats.map((item) => (
                    <div class="stat-item">
                        <h2>{item.value}</h2>
                        <p>{item.label}</p>
                    </div>
                ))}
                <div class="stat-btn-wrapper">
                    <a href={getLink("people")} class="btn btn-stats">Meet our CAGers</a>
                </div>
            </div>
        </div>
    </div>

    <section class="pubs-section">
        <div class="container">
            <h2 class="section-title">Selected publications</h2>
            <a href={getLink("pubs")} class="btn btn-dark-outline">More Contributions</a>
            
            <div class="pub-grid-home">
                {pubs.map((paper) => (
                    <PublicationCard 
                        title={paper.title || "Untitled"}
                        year={paper.year || "202X"}
                        category={paper.category || "Journal"}
                        image={getLink(paper.image ? paper.image : "assets/background/paper.png")}
                        link={paper.link}
                    />
                ))}
            </div>
        </div>
    </section>

    <div class="resource-section" style={`background-image: url(${getLink("assets/background/server.png")})`}>
        <div class="container">
            <h2 class="section-title">Adequate Resource and Facilities</h2>
            <p class="resource-p">
                Equipped with high performance computing workstations, 
                the lab provides an ideal environment for pioneering research and innovation.
            </p>
            <a href={getLink("resource")} class="btn">More Information</a>
        </div>
    </div>

    <section class="talents-section">
        <div class="container">
            <div class="talents-grid">
                <div class="talents-content">
                    <h2 class="section-title">Calling For Talents</h2>
                    <p class="talents-text">Contact us, if any interests in open positions!</p>
                    <a href={getLink("join")} class="btn">Send Message</a>
                </div>
                <div class="talents-visual">
                    <img src={getLink("assets/background/chip.png")} alt="Talent Network">
                </div>
            </div>
        </div>
    </section>

</Layout>

<style>
    .research-grid { display: flex; align-items: center; gap: 60px; }
    .research-text { flex: 1; }
    .research-text p {
        text-align: justify;
        line-height: 1.8;
        margin-bottom: 24px;
        color: var(--text-light);
        font-size: 1.0rem;
    }
    .research-text p:last-of-type {
        margin-bottom: 30px;
    }
    .research-img { flex: 1; text-align: center; }
    .brain-img { width: 100%; max-width: 500px; filter: drop-shadow(0 10px 20px rgba(0,0,0,0.1)); }

    .resource-section { 
        background-size: cover;
        background-position: center;
        height: 500px; 
        display: flex; 
        align-items: center; 
        color: white; 
        position: relative; 
    }
    .resource-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.4);
        z-index: 1;
    }
    .resource-section .container {
        position: relative;
        z-index: 2;
    }
    .resource-title { font-size: 36px; font-weight: 600; margin-bottom: 5px; }
    
    .pubs-section { padding-bottom: 40px; }
    .btn-dark-outline { background: var(--primary-blue); margin-bottom: 40px; }
    .pub-grid-home { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }

    .team-section { 
        background-size: cover;
        background-position: center;
        height: 600px; 
        display: flex; 
        align-items: flex-end; 
        padding-bottom: 60px; 
    }
    .stats-bar { background: rgba(0, 0, 0, 0.75); backdrop-filter: blur(10px); border-radius: 10px; padding: 30px 50px; color: white; display: flex; justify-content: space-between; align-items: center; width: 100%; }
    .stat-item h2 { font-size: 48px; font-weight: 700; line-height: 1; margin-bottom: 5px; }
    .stat-item p { font-size: 12px; letter-spacing: 1px; opacity: 0.8; }
    .btn-stats { margin: 0; }

    .talents-section { background: #f9f9f9; }
    .talents-grid { display: flex; align-items: center; justify-content: space-between; }
    .talents-content { max-width: 400px; }
    .talents-visual img { max-width: 500px; }

    @media (max-width: 1024px) { .pub-grid-home { grid-template-columns: repeat(2, 1fr); } .stats-bar { flex-wrap: wrap; gap: 30px; justify-content: center; text-align: center; } }
    @media (max-width: 768px) { .research-grid, .talents-grid { flex-direction: column; text-align: center; } .pub-grid-home { grid-template-columns: 1fr; } }
</style>
